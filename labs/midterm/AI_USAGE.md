# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** ปฐมพร หลีจาง **รหัส:** 68543210034-3 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 15:30 | B2 | ช่วยอธิบายวิธีทำเช็คลิสต์ในไฟล์ Markdown (`SUBMISSION.md`) หน่อยครับ | อธิบายว่าต้องพิมพ์ตัว `x` ลงไประหว่างวงเล็บเหลี่ยม `[ ]` | พิมพ์ `x` ลงในเช็คลิสต์ของไฟล์ `SUBMISSION.md` · ตรวจสอบว่าช่องสี่เหลี่ยมเปลี่ยนเป็นติ๊กถูกเรียบร้อย |
| 15:45 | B1 | ช่วยไกด์วิธีเขียนอธิบายสาเหตุและวิธีแก้บั๊ก 6 จุด ด้วยคำของตัวเองหน่อย | แนะนำแนวทางคำพูดและสำนวนภาษาไทยสำหรับอธิบายสาเหตุและวิธีแก้ของแต่ละบั๊ก | นำไปปรับเขียนลงในไฟล์ `B1_BUGS.md` ด้วยภาษาของตัวเอง เพื่อเตรียมตัวสอบ Oral |
| 16:15 | B3 | วิธีเพิ่มปุ่ม "รับเรื่อง" เฉพาะการ์ดสถานะ pending และส่ง callback ทำอย่างไร | แนะนำให้ใช้ Short-circuit evaluation (`request.status === 'pending' && <button>`) และส่งผ่าน prop `onAcknowledge` | นำไปเพิ่มโค้ดใน `RequestCard.jsx` และ `RequestList.jsx` · ทดสอบกดดู ปุ่มแสดงเฉพาะการ์ด pending |
| 16:45 | B3 | เขียน handler เรียก updateRequestStatus แล้วอัปเดต state อย่างไร | แนะนำให้สร้างฟังก์ชัน `handleAcknowledge` เรียกใช้ Service และเซ็ตค่า `setRequests(nextRequests)` | นำไปใส่ใน `DashboardPage.jsx` และส่ง prop ลงไป · ทดสอบกดเปลี่ยนสถานะ แผงสรุปอัปเดตทันที |
| 17:05 | B3 | ทำไมคอมมิตไม่ได้ ขึ้น nothing added to commit | อธิบายว่าไม่มีไฟล์ใหม่ที่เปลี่ยนแปลงให้คอมมิตแล้ว เพราะคอมมิตก่อนหน้าสำเร็จไปแล้ว | ตรวจสอบสถานะ Git และเข้าใจว่าสามารถไปลุยงานด่านถัดไปต่อได้เลย |

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ปฐมพร หลีจาง