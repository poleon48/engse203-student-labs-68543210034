# Pre-LAB 05A — ใบจดคำตอบ

**ชื่อ–รหัส:** ______________________
**วันที่ทำ:** ______________________
**Node version (`node -v`):** ______________________
**เบราว์เซอร์ที่ใช้:** ______________________

> เติมช่องว่างด้วยคำพูดของตัวเอง · ข้อที่มีเครื่องหมาย ◆ ตอบไม่ได้ก็ไม่เป็นไร ให้เขียนสิ่งที่เดาไว้
> **เอาใบนี้มาด้วยในวันเรียน**

---

## กิจกรรม A — ทายก่อนกด

**ทายก่อน** ว่าแต่ละ URL จะแสดงหน้าอะไร แล้วค่อยพิมพ์ดูจริง

| URL | ฉันทายว่าจะเห็น | สิ่งที่เห็นจริง | ตรงไหม |
|---|---|---|---|
| `#/` | | | |
| `#/tasks/TASK-101` | | | |
| `#/tasks/TASK-999` | | | |
| `#/about` | | | |
| `#/unknown` | | | |

**A1.** สองบรรทัดสุดท้าย (`#/tasks/TASK-999` และ `#/unknown`) หน้าจอเหมือนกันหรือต่างกัน

```


```

**A2.** ถ้าต่างกัน ต่างกันตรงไหน — อธิบายจากสิ่งที่เห็น

```


```

**A3 ◆** ทำไมนักพัฒนาถึงตั้งใจให้สองกรณีนี้แสดงคนละแบบ ทั้งที่ผู้ใช้ก็ "หาไม่เจอ" เหมือนกัน

```


```

---

## กิจกรรม B — เปิด DevTools ดูว่าอะไรถูกโหลด

**B1.** ตอนกดสลับหน้า มีไฟล์ `.html` ถูกโหลดใหม่หรือไม่

```


```

**B2.** กด `F5` แล้วดู Network อีกครั้ง — ต่างจากตอนกดลิงก์อย่างไร

```


```

**B3 ◆** ถ้าเว็บนี้มีแค่ไฟล์ `index.html` ไฟล์เดียว แล้วมันเปลี่ยนหน้าได้อย่างไร

```


```

---

## กิจกรรม C — หาว่าอะไรอยู่ที่ไหน

| ค้นหาคำว่า | อยู่ในไฟล์ |
|---|---|
| `HashRouter` | |
| `Route` | |
| `Outlet` | |
| `useParams` | |

**C1.** ในไฟล์ที่มีคำว่า `Route` มีกี่บรรทัดที่ขึ้นต้นด้วย `<Route`

```


```

**C2 ◆** `Outlet` อยู่ในไฟล์ที่ทำหน้าที่เป็น "กรอบ" ของทุกหน้า คุณคิดว่ามันทำหน้าที่อะไร

```


```

**C3 ◆** ค่าหลัง `path=` มีเครื่องหมาย `#` อยู่ด้วยไหม แล้วทำไม URL บนเบราว์เซอร์ถึงมี `#`

```


```

---

## กิจกรรม D — สังเกตว่าอะไรเปลี่ยนเมื่อไหร่

| สิ่งที่เกิดขึ้น | เกิดตอนไหน (คำพูดของตัวเอง) |
|---|---|
| ข้อความบนแท็บเบราว์เซอร์เปลี่ยน | |
| ข้อความบนหน้าจอเปลี่ยนหลังกดปุ่ม | |
| รายการ task ถูกกรองตามเงื่อนไข | |

**D1 ◆** ในสามอย่างข้างบน มีอันไหนที่ "ไม่ได้เกิดจากการกดปุ่ม" บ้าง

```


```

---

## Exit Ticket

**1.** URL หนึ่งอันที่คุณลอง และหน้าที่มันแสดง

```


```

**2.** ตอนกดสลับหน้า Network โหลดไฟล์ `.html` ใหม่หรือไม่

```


```

**3.** `#/tasks/TASK-999` กับ `#/unknown` แสดงเหมือนหรือต่างกัน

```


```

**4. คำถามที่คุณยังไม่เข้าใจ อย่างน้อย 1 ข้อ** ← ข้อนี้สำคัญที่สุด

```


```

> ข้อ 4 ไม่มีคำตอบผิด และผู้สอนจะเอาคำถามของพวกคุณมาตอบจริง ๆ ในคาบ
> ถ้าเขียนว่า "ไม่มีคำถาม" แสดงว่ายังสังเกตไม่พอ ลองย้อนกลับไปดูข้อ ◆ อีกครั้ง

---

## เช็คก่อนเข้าคาบ

- [ ] `npm ci` และ `npm run dev` ผ่าน
- [ ] ลอง URL ครบทั้ง 5 อัน และจดสิ่งที่เห็น
- [ ] เปิด DevTools แท็บ Network แล้วสังเกตตอนกดสลับหน้า
- [ ] หาไฟล์ที่มี `HashRouter`, `Route`, `Outlet`, `useParams` เจอครบ
- [ ] ตอบ Exit Ticket ครบ 4 ข้อ
- [ ] **เอาใบนี้มาด้วยในวันเรียน**

**เวลารวมที่ควรใช้ ประมาณ 30 นาที** — ถ้าใช้เกิน 45 นาทีแสดงว่าพยายามหาคำตอบของข้อ ◆ มากเกินไป หยุดได้เลย นั่นคืองานของคาบเรียน
